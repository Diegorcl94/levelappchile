<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base usado por el Manifest -->
    <style name="Theme.LevelAppChile" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Opcional: barras transparentes si usas fondos full-screen -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>